#pragma once

// Copy this file to LocalConfig.h before compiling. LocalConfig.h is ignored
// by Git so the real access-point password stays only on the development PC.
constexpr char AP_SSID[] = "ESP32_Balance";
constexpr char AP_PASSWORD[] = "CHANGE_ME_8_CHARS";
